<%= form_with(model: goal_int, local: true) do |form| %>
  <% if goal_int.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal_int.errors.count, "error") %> prohibited this goal_int from being saved:</h2>

      <ul>
      <% goal_int.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_goal_int %>
    <%= form.text_field :name_goal_int, id: :goal_int_name_goal_int %>
  </div>

  <div class="field">
    <%= form.label :year_goal_int %>
    <%= form.number_field :year_goal_int, id: :goal_int_year_goal_int %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
